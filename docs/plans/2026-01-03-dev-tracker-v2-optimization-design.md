# Dev Tracker v2 優化設計文檔

> 日期: 2026-01-03
> 狀態: 已確認，待實作
> 目標: 個人工具，架構支援未來產品化

---

## 1. 設計摘要

### 優化目標

| 功能 | 描述 | 優先級 |
|------|------|--------|
| 自動掃描 | 啟動時 + 定時掃描 | 高 |
| AI 主動化 | 日常分析 + 異常偵測 | 高 |
| 快捷輸入 | Quick Entry 視窗 + 全局快捷鍵 | 高 |
| 通知系統 | App 內 + macOS 原生 | 中 |
| Slash 指令 | 完整指令系統 | 中 |
| Kanban 視圖 | 三欄看板 (Backlog/進行中/完成) | 中 |

### 設計原則

- **YAGNI**: 只做需要的功能，不過度設計
- **可擴展**: 架構支援未來產品化
- **深夜工作室主題**: 維持一致的 Dark Brutalist Terminal 風格

---

## 2. 整體架構

```
┌─────────────────────────────────────────────────────────────────┐
│                    Dev Tracker v2 優化架構                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐    │
│  │   Frontend   │────▶│  Tauri IPC   │────▶│    Rust      │    │
│  │   (React)    │◀────│   Bridge     │◀────│   Backend    │    │
│  └──────────────┘     └──────────────┘     └──────────────┘    │
│         │                                         │             │
│         ▼                                         ▼             │
│  ┌──────────────┐                         ┌──────────────┐     │
│  │ Quick Entry  │                         │  Scheduler   │     │
│  │   Window     │                         │  (定時任務)   │     │
│  └──────────────┘                         └──────────────┘     │
│         │                                         │             │
│         │              ┌──────────────┐           │             │
│         └─────────────▶│   SQLite     │◀──────────┘             │
│                        │   Database   │                         │
│                        └──────────────┘                         │
│                               │                                 │
│                               ▼                                 │
│                        ┌──────────────┐     ┌──────────────┐   │
│                        │  AI Agent    │────▶│   macOS      │   │
│                        │  (Grok API)  │     │ Notification │   │
│                        └──────────────┘     └──────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 新增模組

- **Scheduler**: Rust 端定時任務管理（掃描、AI 分析）
- **Quick Entry Window**: 獨立的快捷輸入視窗
- **Notification Service**: 統一的通知管理層
- **Slash Commands**: 指令解析與執行
- **Anomaly Detector**: 異常偵測引擎

---

## 3. 自動掃描系統

### 掃描觸發機制

```
┌─────────────────────────────────────────────────────────────┐
│                      Scheduler 模組                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   App 啟動                                                  │
│      │                                                      │
│      ▼                                                      │
│   ┌─────────────────┐    是    ┌─────────────────┐         │
│   │ 上次掃描 > 1hr? │────────▶│  執行全專案掃描  │         │
│   └─────────────────┘          └─────────────────┘         │
│      │ 否                              │                    │
│      ▼                                 ▼                    │
│   跳過掃描                      儲存掃描結果                 │
│                                        │                    │
│   ════════════════════════════════════════════════════     │
│                                                             │
│   定時器 (每 30 分鐘)                                        │
│      │                                                      │
│      ▼                                                      │
│   ┌─────────────────┐    是    ┌─────────────────┐         │
│   │ 有專案需要掃描?  │────────▶│  掃描有變更專案  │         │
│   └─────────────────┘          └─────────────────┘         │
│                                        │                    │
│                                        ▼                    │
│                                 觸發 AI 分析                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Rust 實作結構

```rust
// src-tauri/src/scheduler.rs

pub struct Scheduler {
    scan_interval: Duration,      // 預設 30 分鐘
    last_scan: HashMap<String, DateTime<Local>>,
    is_running: AtomicBool,
}

impl Scheduler {
    pub async fn on_app_start(&self, db: &Database) -> Result<ScanReport>
    pub async fn start_periodic_scan(&self, interval_mins: u64)
    async fn scan_project(&self, project: &Project) -> Result<ScanResult>
    async fn trigger_ai_analysis(&self, results: Vec<ScanResult>)
}
```

### 掃描配置

| 設定項 | 預設值 | 說明 |
|--------|--------|------|
| `scan_interval_mins` | 30 | 定時掃描間隔 |
| `scan_on_startup` | true | 啟動時是否掃描 |
| `min_scan_gap_mins` | 60 | 最小掃描間隔 |
| `scan_inactive_projects` | false | 是否掃描停滯專案 |

---

## 4. AI Agent 主動化系統

### 雙引擎架構

```
┌─────────────────────────────────────────────────────────────────┐
│                    AI Agent 主動化引擎                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Engine A: 日常分析引擎                       │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  掃描結果 ───▶ Grok 分類 ───▶ 生成問題 ───▶ Inbox       │   │
│  │                                                         │   │
│  │  觸發條件：每次掃描完成後                                 │   │
│  │                                                         │   │
│  │  問題類型：                                              │   │
│  │  • 「今日改咗 {n} 個 UI 檔案，係做緊介面優化嗎？」        │   │
│  │  • 「偵測到新檔案 {file}，呢個係咩功能？」               │   │
│  │  • 「Commit: {msg} - 想補充更多細節嗎？」                │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Engine B: 異常偵測引擎                       │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  定時檢查 ───▶ 異常規則 ───▶ 生成提醒 ───▶ Notification │   │
│  │                                                         │   │
│  │  異常規則：                                              │   │
│  │  • TODO 停滯 > 3 天 → 「{task} 仲做緊嗎？」             │   │
│  │  • 專案停滯 > 7 天 → 「{project} 好耐無更新，要封存？」  │   │
│  │  • 連續工作 > 4 小時 → 「記得休息下！」                  │   │
│  │  • Inbox 未讀 > 5 個 → 「有 {n} 個問題等緊你回覆」       │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### AI Prompt 模板

```rust
// src-tauri/src/ai_agent.rs

const DAILY_ANALYSIS_PROMPT: &str = r#"
你是一個開發進度追蹤助手。根據以下 Git 變更分析用戶今日的工作：

專案: {project_name}
變更檔案:
{file_changes}

最近 Commits:
{recent_commits}

請：
1. 用一句話總結今日工作
2. 分類工作類型 (UI/Feature/BugFix/Refactor/Docs/Config)
3. 生成 1-2 個追問問題（幫助用戶補充細節）

回應格式 (JSON):
{
  "summary": "...",
  "category": "...",
  "questions": ["...", "..."]
}
"#;
```

### Inbox Item 結構擴展

```typescript
interface InboxItem {
  id: string;
  item_type: 'daily_analysis' | 'anomaly' | 'reminder' | 'manual';
  question: string;
  context?: string;
  source_scan_id?: string;
  priority: 'low' | 'medium' | 'high';
  auto_generated: boolean;
  status: 'pending' | 'answered';
  answer?: string;
  project_id?: string;
  created_at: string;
  answered_at?: string;
}
```

---

## 5. 快捷輸入 + 全局快捷鍵

### Quick Entry 視窗設計

```
┌─────────────────────────────────────────────────────────────┐
│                                                    ─  □  ✕  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ⚡ Quick Entry                              ⌘ + Shift + D  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │  今日完成咗登入頁面嘅 UI，加埋 validation...       │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────┐  ┌─────────────────────────────────────────┐  │
│  │ 專案 ▼  │  │ 🎮 PeakShift-Overdrive               │  │
│  └─────────┘  └─────────────────────────────────────────┘  │
│                                                             │
│  快捷操作:                                                  │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐              │
│  │ 💬 記錄│ │ ✅ TODO│ │ 💡 想法│ │ 🐛 Bug │              │
│  └────────┘ └────────┘ └────────┘ └────────┘              │
│                                                             │
│                                    [ 取消 ]  [ 送出 ⌘↵ ]   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Tauri 多視窗實作

```rust
// src-tauri/src/quick_entry.rs

use tauri::{WebviewUrl, WebviewWindowBuilder};

pub fn create_quick_entry_window(app: &AppHandle) -> Result<()> {
    let window = WebviewWindowBuilder::new(
        app,
        "quick-entry",
        WebviewUrl::App("quick-entry.html".into())
    )
    .title("Quick Entry")
    .inner_size(500.0, 280.0)
    .resizable(false)
    .decorations(false)
    .always_on_top(true)
    .center()
    .visible(false)
    .build()?;

    Ok(())
}

pub fn register_global_shortcut(app: &AppHandle) -> Result<()> {
    app.global_shortcut().on_shortcut("CmdOrCtrl+Shift+D", |app, _| {
        if let Some(window) = app.get_webview_window("quick-entry") {
            if window.is_visible().unwrap_or(false) {
                window.hide().ok();
            } else {
                window.show().ok();
                window.set_focus().ok();
            }
        }
    })?;

    Ok(())
}
```

### 鍵盤快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| `⌘ + Shift + D` | 開啟/關閉 Quick Entry |
| `⌘ + Enter` | 送出 |
| `Esc` | 取消並關閉 |
| `⌘ + 1/2/3/4` | 快速選擇類型 |
| `Tab` | 切換專案選擇 |

---

## 6. Slash 指令系統

### 指令清單

```
┌─────────────────────────────────────────────────────────────────┐
│                      Slash 指令系統                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📊 查詢類                                                      │
│  ├── /status          今日工作進度摘要                          │
│  ├── /todo            顯示 TODO 列表                            │
│  ├── /stats           統計數據 (本週/本月)                       │
│  └── /plan            本週規劃總覽                               │
│                                                                 │
│  ✏️ 操作類                                                      │
│  ├── /add <task>      新增 TODO                                 │
│  ├── /done <id|name>  標記完成                                  │
│  ├── /move <id> <col> 移動到 Kanban 欄位                        │
│  └── /scan            手動觸發掃描                               │
│                                                                 │
│  🔧 系統類                                                      │
│  ├── /project <name>  切換當前專案                               │
│  ├── /projects        列出所有專案                               │
│  └── /help            顯示指令說明                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 實作結構

```rust
// src-tauri/src/slash_commands.rs

#[derive(Debug)]
pub enum SlashCommand {
    // 查詢類
    Status,
    Todo { filter: Option<String> },
    Stats { period: StatsPeriod },
    Plan,

    // 操作類
    Add { task: String, project_id: Option<String> },
    Done { identifier: String },
    Move { task_id: String, column: KanbanColumn },
    Scan { project_id: Option<String> },

    // 系統類
    Project { name: String },
    Projects,
    Help,
}

pub fn parse_command(input: &str) -> Option<SlashCommand> {
    let input = input.trim();
    if !input.starts_with('/') {
        return None;
    }

    let parts: Vec<&str> = input[1..].splitn(2, ' ').collect();
    let cmd = parts[0].to_lowercase();
    let args = parts.get(1).map(|s| s.trim());

    match cmd.as_str() {
        "status" => Some(SlashCommand::Status),
        "todo" => Some(SlashCommand::Todo { filter: args.map(String::from) }),
        "stats" => Some(SlashCommand::Stats { period: parse_period(args) }),
        "plan" => Some(SlashCommand::Plan),
        "add" => args.map(|a| SlashCommand::Add {
            task: a.to_string(),
            project_id: None
        }),
        "done" => args.map(|a| SlashCommand::Done {
            identifier: a.to_string()
        }),
        "scan" => Some(SlashCommand::Scan { project_id: args.map(String::from) }),
        "project" => args.map(|a| SlashCommand::Project { name: a.to_string() }),
        "projects" => Some(SlashCommand::Projects),
        "help" => Some(SlashCommand::Help),
        _ => None,
    }
}
```

---

## 7. 通知系統

### 通知架構

```
┌─────────────────────────────────────────────────────────────────┐
│                      統一通知服務                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                    ┌─────────────────┐                          │
│                    │ NotificationHub │                          │
│                    └────────┬────────┘                          │
│                             │                                   │
│         ┌───────────────────┼───────────────────┐              │
│         │                   │                   │              │
│         ▼                   ▼                   ▼              │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐        │
│  │  App 內部   │    │ macOS 原生  │    │   Badge     │        │
│  │   Toast     │    │   通知中心   │    │  (Dock)     │        │
│  └─────────────┘    └─────────────┘    └─────────────┘        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 通知類型與策略

| 事件 | App Toast | macOS 通知 | Badge | 優先級 |
|------|-----------|------------|-------|--------|
| 掃描完成 | ✅ | ❌ | ❌ | low |
| AI 生成問題 | ✅ | ✅ | ✅ +1 | medium |
| TODO 停滯提醒 | ✅ | ✅ | ❌ | medium |
| 專案停滯警告 | ✅ | ✅ | ❌ | high |
| 操作成功 | ✅ | ❌ | ❌ | low |
| 錯誤發生 | ✅ | ❌ | ❌ | high |

### Rust 實作

```rust
// src-tauri/src/notification.rs

use tauri_plugin_notification::NotificationExt;

#[derive(Clone, Debug)]
pub enum NotifyLevel {
    Low,
    Medium,
    High,
}

pub struct NotificationHub {
    app: AppHandle,
}

impl NotificationHub {
    pub fn new(app: AppHandle) -> Self {
        Self { app }
    }

    pub fn send(&self, title: &str, body: &str, level: NotifyLevel) {
        // 1. App 內 Toast
        self.app.emit("notification", NotificationPayload {
            title: title.to_string(),
            body: body.to_string(),
            level: level.clone(),
        }).ok();

        // 2. macOS 系統通知
        if matches!(level, NotifyLevel::Medium | NotifyLevel::High) {
            self.app.notification()
                .builder()
                .title(title)
                .body(body)
                .show()
                .ok();
        }

        // 3. Badge 更新
        self.update_badge();
    }
}
```

### 用戶通知偏好設定

```typescript
interface NotificationSettings {
  enable_system_notifications: boolean;
  enable_badge: boolean;
  quiet_hours: {
    enabled: boolean;
    start: string;  // "22:00"
    end: string;    // "08:00"
  };
  notify_on_scan_complete: boolean;
  notify_on_ai_question: boolean;
  notify_on_todo_stale: boolean;
}
```

---

## 8. Kanban 看板視圖

### 看板設計

```
┌─────────────────────────────────────────────────────────────────────────┐
│  📋 TODO Kanban                                    🔍 搜尋  ➕ 新增     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐         │
│  │ 📥 Backlog (5)  │  │ 🔄 進行中 (2)   │  │ ✅ 完成 (8)     │         │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤         │
│  │                 │  │                 │  │                 │         │
│  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │         │
│  │ │ 音效系統    │ │  │ │ 登入頁 UI  │ │  │ │ 卡牌動畫    │ │         │
│  │ │ #PeakShift  │ │  │ │ #PeakShift  │ │  │ │ #PeakShift  │ │         │
│  │ │ 🏷️ Feature  │ │  │ │ 🏷️ UI      │ │  │ │ ✓ 今日完成  │ │         │
│  │ └─────────────┘ │  │ └─────────────┘ │  │ └─────────────┘ │         │
│  │                 │  │                 │  │                 │         │
│  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │         │
│  │ │ 成就系統    │ │  │ │ 計分 Bug   │ │  │ │ 專案設定    │ │         │
│  │ │ #PeakShift  │ │  │ │ #PeakShift  │ │  │ │ #Portfolio  │ │         │
│  │ │ 🏷️ Feature  │ │  │ │ 🏷️ BugFix  │ │  │ │ ✓ 昨日完成  │ │         │
│  │ │ ⚠️ 停滯3天  │ │  │ └─────────────┘ │  │ └─────────────┘ │         │
│  │ └─────────────┘ │  │                 │  │                 │         │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘         │
│                                                                         │
│  ┌─ 篩選 ──────────────────────────────────────────────────────────┐   │
│  │ 專案: [ 全部 ▼ ]   標籤: [ 全部 ▼ ]   排序: [ 更新時間 ▼ ]      │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 資料結構擴展

```typescript
type KanbanColumn = 'backlog' | 'in_progress' | 'done';

interface Todo {
  id: string;
  title: string;
  description?: string;
  project_id?: string;
  column: KanbanColumn;
  position: number;
  labels: string[];
  due_date?: string;
  stale_days?: number;
  created_at: string;
  updated_at: string;
  completed_at?: string;
}
```

### 資料庫遷移

```sql
-- migrations/003_kanban_columns.sql

ALTER TABLE todos ADD COLUMN column TEXT DEFAULT 'backlog';
ALTER TABLE todos ADD COLUMN position INTEGER DEFAULT 0;
ALTER TABLE todos ADD COLUMN labels TEXT DEFAULT '[]';
ALTER TABLE todos ADD COLUMN due_date TEXT;

UPDATE todos SET column = 'done' WHERE status = 'completed';
UPDATE todos SET column = 'backlog' WHERE status = 'pending';

CREATE INDEX idx_todos_column ON todos(column);
CREATE INDEX idx_todos_project_column ON todos(project_id, column);
```

### React 組件結構

```
src/components/kanban/
├── KanbanBoard.tsx
├── KanbanColumn.tsx
├── KanbanCard.tsx
├── KanbanFilters.tsx
├── AddTaskModal.tsx
└── index.ts
```

---

## 9. 分階段實作計劃

### 總覽

```
Phase 1: 基礎自動化          Phase 2: 智能體驗         Phase 3: 進階功能
════════════════════        ════════════════════      ════════════════

┌─────────────────┐        ┌─────────────────┐      ┌─────────────────┐
│ • Scheduler     │        │ • Quick Entry   │      │ • Kanban View   │
│ • 自動掃描      │   →    │ • 全局快捷鍵    │  →   │ • 進階篩選      │
│ • AI 日常分析   │        │ • Slash 指令    │      │ • 資料匯出      │
│ • 基本通知      │        │ • 異常偵測      │      │ • 主題自訂      │
└─────────────────┘        └─────────────────┘      └─────────────────┘

      MVP 核心                  完整體驗                 錦上添花
```

---

### Phase 1: 基礎自動化 (MVP)

**目標**: 實現核心自動化，讓 App 變得「主動」

**檔案變更清單**:

新增檔案:
- `src-tauri/src/scheduler.rs` - 定時任務管理
- `src-tauri/src/ai_agent.rs` - AI 主動分析邏輯
- `src-tauri/src/notification.rs` - 通知服務
- `src/components/Toast/EnhancedToast.tsx` - 增強 Toast

修改檔案:
- `src-tauri/src/lib.rs` - 註冊新模組
- `src-tauri/src/models.rs` - InboxItem 擴展
- `src-tauri/src/db.rs` - 新增欄位遷移
- `src-tauri/Cargo.toml` - 新增依賴
- `src/stores/settingsStore.ts` - 掃描設定
- `src/pages/Settings.tsx` - 設定 UI

**任務清單**:

| # | 任務 | 依賴 | 複雜度 |
|---|------|------|--------|
| 1.1 | 建立 `scheduler.rs` 基礎結構 | - | 低 |
| 1.2 | 實作啟動時掃描邏輯 | 1.1 | 低 |
| 1.3 | 實作定時掃描 (tokio interval) | 1.1 | 中 |
| 1.4 | 建立 `ai_agent.rs` 分析引擎 | - | 中 |
| 1.5 | 設計 AI Prompt 模板 | 1.4 | 低 |
| 1.6 | 實作掃描後自動生成 Inbox | 1.3, 1.4 | 中 |
| 1.7 | 建立 `notification.rs` 服務 | - | 低 |
| 1.8 | 整合 tauri-plugin-notification | 1.7 | 低 |
| 1.9 | 擴展 Settings UI (掃描設定) | 1.3 | 低 |
| 1.10 | 測試與除錯 | 全部 | 中 |

---

### Phase 2: 智能體驗

**目標**: 提升使用效率，快捷輸入 + 指令系統

**檔案變更清單**:

新增檔案:
- `src-tauri/src/quick_entry.rs` - Quick Entry 視窗管理
- `src-tauri/src/slash_commands.rs` - 指令解析器
- `src-tauri/src/anomaly_detector.rs` - 異常偵測引擎
- `src/quick-entry/` - Quick Entry 前端
- `src/components/chat/CommandResponse.tsx` - 指令回應組件
- `quick-entry.html` - 獨立入口

修改檔案:
- `src-tauri/src/lib.rs` - 多視窗 + 快捷鍵
- `src-tauri/tauri.conf.json` - 視窗配置
- `src/pages/Chat.tsx` - Slash 指令整合
- `src/stores/chatStore.ts` - 指令處理邏輯
- `package.json` - 新增依賴

**任務清單**:

| # | 任務 | 依賴 | 複雜度 |
|---|------|------|--------|
| 2.1 | 建立 `quick_entry.rs` 視窗管理 | - | 中 |
| 2.2 | 註冊全局快捷鍵 ⌘+Shift+D | 2.1 | 低 |
| 2.3 | 建立 Quick Entry 前端頁面 | 2.1 | 中 |
| 2.4 | 實作快捷類型選擇 (記錄/TODO/想法) | 2.3 | 低 |
| 2.5 | 建立 `slash_commands.rs` 解析器 | - | 中 |
| 2.6 | 實作查詢類指令 (/status /todo /stats) | 2.5 | 中 |
| 2.7 | 實作操作類指令 (/add /done /scan) | 2.5 | 中 |
| 2.8 | Chat UI 整合 Slash 指令 | 2.6, 2.7 | 低 |
| 2.9 | 建立 `anomaly_detector.rs` | 1.4 | 中 |
| 2.10 | 實作停滯偵測規則 | 2.9 | 低 |
| 2.11 | 測試與除錯 | 全部 | 中 |

---

### Phase 3: 進階功能

**目標**: Kanban 視圖 + 體驗優化

**檔案變更清單**:

新增檔案:
- `src/components/kanban/` - Kanban 組件
- `src/pages/Kanban.tsx` - Kanban 頁面
- `migrations/003_kanban_columns.sql` - DB 遷移

修改檔案:
- `src-tauri/src/db.rs` - Kanban 欄位
- `src-tauri/src/models.rs` - Todo 擴展
- `src-tauri/src/commands.rs` - move_todo 指令
- `src/lib/types.ts` - 類型定義
- `src/stores/todoStore.ts` - Kanban 邏輯
- `src/App.tsx` - 路由
- `src/components/layout/Sidebar.tsx` - 導航
- `package.json` - dnd-kit

**任務清單**:

| # | 任務 | 依賴 | 複雜度 |
|---|------|------|--------|
| 3.1 | 設計 Kanban 資料結構 | - | 低 |
| 3.2 | 資料庫遷移 (column, position) | 3.1 | 低 |
| 3.3 | 建立 KanbanBoard 組件 | 3.2 | 中 |
| 3.4 | 建立 KanbanColumn 組件 | 3.3 | 低 |
| 3.5 | 建立 KanbanCard 組件 | 3.4 | 低 |
| 3.6 | 整合 dnd-kit 拖放 | 3.3-3.5 | 高 |
| 3.7 | 實作 move_todo Rust 指令 | 3.2 | 中 |
| 3.8 | 建立篩選功能 | 3.3 | 中 |
| 3.9 | 整合 /move Slash 指令 | 3.7, 2.5 | 低 |
| 3.10 | 深夜工作室主題樣式 | 3.3-3.5 | 中 |
| 3.11 | 測試與除錯 | 全部 | 中 |

---

## 10. 新增依賴清單

### Rust (Cargo.toml)

```toml
[dependencies]
# Phase 1
tauri-plugin-notification = "2"
tokio = { version = "1", features = ["time", "sync"] }

# Phase 2
tauri-plugin-global-shortcut = "2"
```

### JavaScript (package.json)

```json
{
  "dependencies": {
    "@dnd-kit/core": "^6.1.0",
    "@dnd-kit/sortable": "^8.0.0",
    "@dnd-kit/utilities": "^3.2.2"
  }
}
```

---

## 11. 後續考量

### 產品化時需要補充

- [ ] 多語言支援 (i18n)
- [ ] 用戶 onboarding 流程
- [ ] 錯誤追蹤 (Sentry)
- [ ] 自動更新機制
- [ ] 資料備份/匯出
- [ ] 隱私政策與使用條款

### 潛在擴展方向

- GitHub/GitLab 整合
- 團隊協作功能
- 自訂 AI 模型 (OpenAI/Claude)
- 跨平台支援 (Windows/Linux)
- 行動端 companion app

---

> 文檔版本: 1.0
> 最後更新: 2026-01-03
